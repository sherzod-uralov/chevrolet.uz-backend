interface users {
    username:String
    password:String
}

interface contextType {
    authToken:any
}

interface category {
    category_name:String
}

interface category_id {
    category_id:Number
    category_name:String
}